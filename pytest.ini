[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers = 
    integration: marks tests as integration tests (may require external resources)
    slow: marks tests as slow running (may take more time to complete)
    network: marks tests that require network connectivity

